import styled from '@emotion/styled';

const ProfileContainer = styled.div`
  height: 4rem;
  width: 4rem;

  & img {
    width: 100%;
    border-radius: 50%;
  }
`;

export default ProfileContainer;
